'use strict';

/**
 * A set of functions called "actions" for `<%= globalID %>`
 */

module.exports = {
  /**
   * Index.
   *
   * @return {Object|Array}
   */

  index: async (ctx, next) => {
    ctx.body = {
      message: 'hello'
    };
  }
};
